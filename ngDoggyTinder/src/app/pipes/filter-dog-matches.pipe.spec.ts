import { FilterDogMatchesPipe } from './filter-dog-matches.pipe';

describe('FilterDogMatchesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDogMatchesPipe();
    expect(pipe).toBeTruthy();
  });
});
